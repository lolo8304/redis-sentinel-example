# Environment variables used by the Nest Redis Sentinel example.
# Copy into `nest-redis-sentinel/.env` and adjust for your cluster.

# Redis auth
REDIS_PASSWORD=toto
REDIS_SENTINEL_PASSWORD=

# Sentinel discovery
REDIS_SENTINEL_MASTER_SET=mymaster
REDIS_SENTINEL_HOSTS=127.0.0.1:26381 # comma-separated host:port list; use the sentinel service name in-cluster (e.g., sentinel:26379)

# DB selection
REDIS_DB=0

# Connection behavior
REDIS_ENABLE_TLS=false
REDIS_LAZY_CONNECT=false
REDIS_ENABLE_READY_CHECK=false
REDIS_ENABLE_AUTO_PIPELINING=true
REDIS_FAILOVER_DETECTOR=true
REDIS_AUTO_RESUBSCRIBE=true
REDIS_SHOW_FRIENDLY_ERROR_STACK=true
REDIS_ENABLE_OFFLINE_QUEUE=true
REDIS_CONNECT_TIMEOUT_MS=10000
REDIS_MAX_RETRIES_PER_REQUEST=5 # set to "null" for infinite
REDIS_KEEP_ALIVE_MS=30000
REDIS_RETRY_STRATEGY=false
REDIS_SENTINEL_RETRY_STRATEGY=false
REDIS_CACHE_TTL=3600

# Debug: enable verbose ioredis sentinel/client logs (optional)
#DEBUG=ioredis:*

# Optional: overrides for Helm aliases (defaults are fine for local use)
# REDIS_SENTINEL_RELEASE=redis-sentinel
# REDIS_SENTINEL_NS=redis
