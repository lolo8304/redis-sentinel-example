# Environment variables used by the Nest Redis Sentinel example.
# Copy into `nest-redis-sentinel/.env` and adjust for your cluster.

# Redis auth
REDIS_PASSWORD=toto
REDIS_SENTINEL_PASSWORD=

# Sentinel discovery
REDIS_SENTINEL_MASTER_SET=mymaster
#remote. dont use service
REDIS_SENTINEL_HOSTS=my-redis-redis-sentinel-redis-0.redis.default.svc.cluster.local:26379,my-redis-redis-sentinel-redis-1.redis.default.svc.cluster.local:26379,my-redis-redis-sentinel-redis-2.redis.default.svc.cluster.local:26379
#local
REDIS_SENTINEL_HOSTS=127.0.0.1:26381 

# DB selection
REDIS_DB=0

# Connection behavior (defaults align with Helm values.yaml)
REDIS_ENABLE_TLS=false
REDIS_LAZY_CONNECT=true
REDIS_ENABLE_READY_CHECK=true
REDIS_ENABLE_AUTO_PIPELINING=false
REDIS_FAILOVER_DETECTOR=false
REDIS_AUTO_RESUBSCRIBE=false
REDIS_SHOW_FRIENDLY_ERROR_STACK=true
REDIS_ENABLE_OFFLINE_QUEUE=true
REDIS_CONNECT_TIMEOUT_MS=10000
REDIS_MAX_RETRIES_PER_REQUEST=200 # set to "null" for infinite
REDIS_KEEP_ALIVE_MS=10000
REDIS_RETRY_STRATEGY=true
REDIS_SENTINEL_RETRY_STRATEGY=true
REDIS_CACHE_TTL=3600

# Debug: enable verbose ioredis sentinel/client logs (optional)
#DEBUG=ioredis:*

# Optional: overrides for Helm aliases (defaults are fine for local use)
# REDIS_SENTINEL_RELEASE=redis-sentinel
# REDIS_SENTINEL_NS=redis
